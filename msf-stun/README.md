# Session Traversal Utilities for NAT

Pure Rust implementation of STUN message encoder/decoder based on RFC 5389 and
RFC 8489 with optional extensions for Interactive Connectivity Establishment
(ICE) and Traversal Using Relays around NAT (TURN).

## Features

* `ice` - enable the ICE extension
* `turn` - enable the TURN extension
